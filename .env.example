# .env — copy to `.env` (gitignored) and fill in.

# Optional: only needed if you re-introduce Gemini AI features.
# (Currently the codebase doesn't use any Gemini APIs — left here as a hook.)
# GEMINI_API_KEY="your-gemini-api-key"
